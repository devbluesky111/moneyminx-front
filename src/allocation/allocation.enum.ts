export enum AllocationsFilter {
  TYPE = 'Type',
  ASSET_CLASS = 'Asset Class',
  COUNTRY = 'Country',
  RISK = 'Risk',
}

export enum AllocationSectionEnum {
  MY_ALLOCATION,
  PREVIOUS_ALLOCATION,
  SIMILAR_ALLOCATION,
}
