import React from 'react';
import { AllocationProps } from '../allocation.type';

const Allocation: React.FC<AllocationProps> = () => {
  return <div>we are in Allocation page</div>;
};

export default Allocation;
