import React from 'react';
import { SettingsProps } from './setting.type';

const Settings: React.FC<SettingsProps> = () => {
  return <div>we are in Settings page</div>;
};

export default Settings;
